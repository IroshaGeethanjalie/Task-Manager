# Normalize line endings across OS
* text=auto

# Source code files
*.js     text
*.jsx    text
*.ts     text
*.tsx    text
*.json   text
*.css    text
*.html   text
*.md     text
*.env    text

# Prevent diff on minified files
*.min.js   -diff
*.min.css  -diff

# Image and asset files (binary)
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.svg   binary
*.ico   binary

# Font files (if any)
*.woff   binary
*.woff2  binary
*.ttf    binary
*.eot    binary

# Lock files
package-lock.json merge=union
yarn.lock         merge=union

# Git LFS tracking (if needed)
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.jpg  filter=lfs diff=lfs merge=lfs -text
*.svg  filter=lfs diff=lfs merge=lfs -text
